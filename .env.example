# Firebase Configuration
# Get these values from your Firebase Console: https://console.firebase.google.com/

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# Cloudinary Configuration
# Get these values from your Cloudinary Dashboard: https://cloudinary.com/console

VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=your-upload-preset

# PHP API serves only /api/products (product fetching from MySQL). No cart/checkout/orders.
# For cart and checkout you must use Laravel: set VITE_LARAVEL_API_BASE_URL to your Laravel APP_URL.
VITE_LARAVEL_API_BASE_URL=
# Base URL for products API (PHP or Laravel); used for product listing and images.
VITE_PHP_API_BASE_URL=http://dev.brandip.com:8080/domain-names-for-sale

# Optional: override Laravel checkout paths if your app uses different routes (defaults shown).
# VITE_LARAVEL_CART_PATH=/api/checkout/cart
# VITE_LARAVEL_ADDRESSES_PATH=/api/checkout/onepage/addresses
# VITE_LARAVEL_PAYMENT_PATH=/api/checkout/onepage/payment-methods
# VITE_LARAVEL_ORDERS_PATH=/api/checkout/onepage/orders

# Base URL for product/domain images from Laravel API (image_path is prefixed with this; no double slashes).
# Prefer VITE_IMAGE_BASE_URL; falls back to VITE_CLOUDFRONT_URL, then default S3 URL.
VITE_IMAGE_BASE_URL=https://brandip-domains.s3.us-east-2.amazonaws.com
# VITE_CLOUDFRONT_URL=https://brandip-domains.s3.us-east-2.amazonaws.com
